<%= form_for(@blog, url: "/blogsadmin/2") do |f| %>
 <% if @blog.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@blog.errors.count, "error") %> prohibited this blog from being saved:</h2>

      <ul>
      <% @blog.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :project %><br>
    <%= f.select :project, options_for_select([['Koodikarpät-peli',1],['Java-alkeet',2], ['Lukkari',3],['Robootti',4]]), required: true%>
  </div>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name, required: true %>
  </div>
  <div class="field">
    <%= f.label :subject %><br>
    <%= f.text_field :subject, required: true %>
  </div>
  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_area :content, size: "80x25", required: true %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
